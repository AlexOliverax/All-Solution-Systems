# Contribuindo

## Setup
```bash
npm ci
npm run build
```

## Estrutura
- `index.html` (Home)
- `agendamento/index.html`
- `assets/css/site.css`

## Regra de ouro
Botões/CTAs devem funcionar mesmo sem JS (fallback).
